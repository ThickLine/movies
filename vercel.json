{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/nuxt",
      "config": { "serverFiles": ["proxy/*"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}
