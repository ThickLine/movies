version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn postinstall
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: .output/public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
