version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn run postinstall
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: .output/public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
